let userNames = ['ada', 'pesho', 'Asen','maria'];

// log in console only the names, starting with 'a' or 'A'
for (let index = 0; index < userNames.length; index++) {
	const element = userNames[index];

    if( element.toUpperCase().startsWith('A') ){
        console.log(element);
    }
}




































